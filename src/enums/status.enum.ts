export enum TransactionStatusEnum {
  PENDING = 'pending',
  SUCCESS = 'success',
  FAILED = 'failed',
}

export enum RequestStatusPaymentEnum {
  SUCCESS = 'success',
  FAILED = 'failed',
  NOT_INITIATED = 'not initialized',
  PENDING = 'pending',
}
